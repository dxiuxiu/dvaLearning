const DONE = 'DONE'

const WILLDO = 'WILLDO'

const ADD = 'ADD'

const DEL = 'DEL'

const addAsync = 'addAsync'
const actionType = {
   DONE,
   WILLDO,
   ADD,
   DEL,
   addAsync
}

const listStatus = {
   DONE,
   WILLDO,
}

export {
   actionType,
   listStatus
}
